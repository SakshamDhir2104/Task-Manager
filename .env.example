# Local development (copy to server/.env)
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/team_tasks?schema=public"
JWT_SECRET="change-me-to-a-long-random-string"
PORT=4000
CLIENT_ORIGIN="http://localhost:5173"

# Railway sets DATABASE_URL and PORT automatically.
# Add JWT_SECRET in the Railway service variables.
